#!/bin/bash

echo "🧪 コミット前テスト実行中..."

# テスト実行
mvn test -q

# テストが失敗した場合、コミットを中止
if [ $? -ne 0 ]; then
    echo "❌ テストが失敗しました。コミットを中止します。"
    echo "💡 テストを修正してから再度コミットしてください。"
    exit 1
fi

echo "✅ 全てのテストが成功しました。コミットを続行します。"